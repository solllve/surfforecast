import { OrderWavesPipe } from './order-waves.pipe';

describe('OrderWavesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderWavesPipe();
    expect(pipe).toBeTruthy();
  });
});
